Lane C owns screenshot library features and fake AI tagging flow.
Implement: upload form (local only), list/grid toggle, tags, search/filter UI.
No backend required; use seeded/mock data with clean component architecture.
